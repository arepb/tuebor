---
# Template for a new pledgee profile. Copy this file, rename to <slug>.md, fill in.
# Keep `published: false` until the profile is ready to go live.
layout: pledgee
name: Full Name
last_name: Name                    # REQUIRED for roster sort. GitHub Pages doesn't run custom plugins, so set explicitly.
slug: full-name                    # REQUIRED, unique. Uniqueness enforced by _plugins/check_unique_slugs.rb.
city: City
region: Southeast Michigan         # optional
year_joined: 2026

# Portrait pipeline (see assets/images/_dither.py):
#   1. Drop raw photo in /assets/images/pledgees/_sources/<portrait_source>
#   2. Start with portrait_face_y: 0.5, portrait_scale: 1.0
#   3. Run `python assets/images/_dither.py`; adjust face_y/scale if crop is off
portrait_source: full-name.jpg
portrait_face_y: 0.5               # 0..1 vertical center of face (default 0.5)
portrait_scale: 1.0                # <1 = tighter crop around face (default 1.0)
portrait: /assets/images/pledgees/full-name-halftone.png    # GENERATED
og_image: /assets/images/pledgees/full-name-og.png          # GENERATED by _ogimage_pledgee.py

linkedin: https://www.linkedin.com/in/...
quote: "A one-line pull quote."

# Optional. Only entries with `public: true` render on the profile.
# Omit the entire `backed:` block if every entry would be private.
backed:
  - year: 2026
    company: "Example Co."
    url: "https://example.com"       # optional; if present, company renders as link
    note: "optional one-line context"
    public: true

published: false                   # flip to true when ready to go live
---

150–300 word story here. First person. Why Michigan. Why the pledge. Personal voice.
